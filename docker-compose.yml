services:
  waha:
    image: devlikeapro/waha:latest
    container_name: waha
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - docker/.env
    volumes:
      - waha_instances:/app/.sessions

volumes:
  waha_instances: